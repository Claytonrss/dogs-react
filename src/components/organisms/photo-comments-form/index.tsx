import React from 'react';
import { Container } from './styles';

const PhotoCommentsForm: React.FC<PhotoCommentsFormProps> = ({ id }) => {
  return <Container>Form Comentários</Container>;
};

export default PhotoCommentsForm;
